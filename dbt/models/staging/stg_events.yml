version: 2

models:
  - name: stg_events
    description: This model cleans up events data
    columns:
      - name: event_id
        description: Primary key
        tests:
          - unique
          - not_null